{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sass --watch scss/styles.scss:css/styles.css --style expanded & live-server",
    "startSass": "sass --watch scss/styles.scss:css/styles.css --style expanded",
    "startLS": "live-server"
  },
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.1",
    "sass": "^1.45.0"
  }
}
